meta {
  name: Get Roles as admin
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/admin/users-with-roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}